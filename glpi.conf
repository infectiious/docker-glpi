<Directory /var/www/glpi>
    Options FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

# Prevent access to non-public files and directories
# <FilesMatch "^\.|\/\.">
#     Require all denied
# </FilesMatch>